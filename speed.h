#ifndef SPEED_H
#define SPEED_H

const int FLOATING_CLIP_PIXELS_PER_S = 420; /// The numbers of pixels a clip moves in one second.
const int ROOM_WIND_CHANGE_INTERVAL_MS = 300; /// This constant defines how often the wind changes its direction.
const int ROOM_REPAINT_INTERVAL_MS = 50; /// This constant defines how often the complete room widget with doors and floating clip is repainted.

#endif
